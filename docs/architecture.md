# Architecture

The full design rationale (why a doubly linked list instead of a graph, the BMJ
routing algorithm, the four-tier memory hierarchy, measured latency and memory
footprint on constrained hardware) is documented in the Medium article series:

- "From Agent OS to Agent Processor Unit"
- "Implementing the Agent Processor Unit: A Tutor That Runs Offline"

This file stays short on purpose. Update it once the Nemotron port is running with
whatever changed versus the articles (model routing, any new benchmark numbers),
rather than duplicating the full writeup here.
